scalar DateTime @scalar(class: "Nuwave\\Lighthouse\\Schema\\Types\\Scalars\\DateTime")
directive @paginate  on FIELD_DEFINITION

type User {
  id: ID!
  name: String!
  email: String!
}
type Article {
    id: ID!
    title: String!
    content: String!
    user_id: Int!
    author: User! @belongsTo(relation: "user")
}

type PaginatorInfo {
    currentPage: Int!
    lastPage: Int!
}

type ArticlePaginator {
    data: [Article!]!
    paginatorInfo: PaginatorInfo!
}

type Query {
    me: User
    users: [User!]!
    userById(id: ID): User

    article(id: ID! @eq): Article @find
    #articles(first: Int!, page: Int): ArticlePaginator @paginate

    greet(name: String = "Tona"): [String!]
    hello: [String!]
}

type Mutation {
    createArticle(title: String!, content: String!, user_id: Int!): Article!
    createArticle2(title: String!, content: String!, user_id: Int!): Article! @create
}
